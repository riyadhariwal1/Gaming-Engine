include "InputChoice.h"


InputChoiceRule::InputChoiceRule(string player, string prompt,
  *string choices, string result )
  {
    // send promp to a player with list of choices and
    // save choice in given variable "result"
  }
